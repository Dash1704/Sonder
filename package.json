{
    "scripts": {
      "start": "cd server && npm install && node app.js cd .. cd client && npm install && npm run build"
    },
    "proxy": "http://localhost:8080",
    "homepage": "https://sonder-techforgood.herokuapp.com/"
  }
